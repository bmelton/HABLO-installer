Make all of the mods in this repository also work as close as they possibly can, but using Claude Code instead of the Pi coding agent. 

Update the installer at the beginning to ask: "Which coding agent would you like to use?" 
- Pi Coding Agent
- Claude Code

We should detect whether they are already installed and, if so, then the prompt should read: 

"Which coding agent would you like to use?" 
- Pi Coding Agent (already installed)
- Claude Code (already installed)
